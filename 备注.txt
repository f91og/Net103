1. 关于连接多子站的情形
2. 需要将连接报文改成点对点的形式，而不是广播
3. 告警要根据描述再去后台数据库中取出
4. 遥信变位中的时间没有日期的，需取系统当前日期
5. 保护动作报告 动作参数 每个报告最多20个 怎么存库？
6. 心跳报文 
7. 双连接的情形
8. 传输报文的后续标志位


1. gateway--管理机部分--一个链路连接 多个设备
2. ip地址 2，3字节的问题
3. 关于sta的疑问，在pro103apprivate的start()方法中：DbObjectPtr sta = dev->RelationOne("station");
4. 录波部分要替换
5. 关于qtcpsocket的问题

SlotReadReady()
tcp 中的 PacketReceived信号


